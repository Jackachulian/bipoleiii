# Handles writing lines and receiving input from the webpage.
# Writes lines and creates input buttons.
# Returns input data from buttons to the main code.

# (Don't mind the error here if you're using type checking :))
# idk how to fix it
def write_line(string: str):
    display(string, target="output")